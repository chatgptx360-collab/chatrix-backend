/**
 * Re-export of expo-linear-gradient under a name that matches the import we
 * already use across screens. Centralized so future swap (e.g. SVG gradient
 * for performance) is one-file.
 */
export { LinearGradient as LinearGradientStub } from "expo-linear-gradient";
export { LinearGradient } from "expo-linear-gradient";
