{
  "name": "@chatrix/ui",
  "version": "0.1.0",
  "private": true,
  "description": "Cross-platform brand primitives — design tokens (web + native shared) + small headless components.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./tokens": "./src/tokens.ts"
  },
  "scripts": {
    "build": "echo 'ui consumed as source — no build step'",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'lint: ui (skipped)'",
    "test": "echo 'test: ui (no tests yet)'",
    "clean": "rm -rf .turbo"
  },
  "peerDependencies": {
    "react": "^18.0.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.11",
    "typescript": "^5.6.3"
  }
}
