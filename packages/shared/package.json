{
  "name": "@chatrix/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Cross-platform contract: types, validation schemas, constants, error codes, socket events.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./types": "./src/types/index.ts",
    "./schemas": "./src/schemas/index.ts",
    "./constants": "./src/constants.ts",
    "./errors": "./src/errors.ts",
    "./events": "./src/events.ts",
    "./reactions": "./src/reactions.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'lint: shared (skipped)'",
    "test": "echo 'test: shared (no tests yet)'",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
